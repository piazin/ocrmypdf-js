export * from "@/lib";

/*
import { resolve } from "path";
import { OcrMyPdf } from "@/lib";

(async () => {
  const inputPath = resolve("./public/teste1.pdf");
  const outputPath = resolve("./public/teste1-ocr.pdf");

  const ocrMyPdf = new OcrMyPdf({ inputPath, outputPath });

  ocrMyPdf.execute();
})();
*/
